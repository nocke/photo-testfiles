# do NOT gitignore temp, hidden (`.`), backup files (`thumb`, `~`,...)
# since files named that way are legitimate parts of tests

package-lock.json

/node_modules
Link to*
temp*/
test-folder*/
test-følder*/
